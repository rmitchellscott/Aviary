services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    environment:
      PDF_DIR: /app/pdfs
      RMAPI_HOST: "${RMAPI_HOST}"
    volumes:
      - ./pdfs:/app/pdfs
      - ./rmapi.conf:/root/.config/rmapi/rmapi.conf
